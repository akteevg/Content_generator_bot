"""Prompt builders for interactions with the GigaChat API."""

from __future__ import annotations


IDEA_SYSTEM_PROMPT = (
    "Ты — опытный контент-маркетолог и стратег, который помогает авторам придумывать сильные идеи для публикаций. "
    "Учитывай нишу, цель и формат, предлагай свежие и применимые концепты."
)


POST_SYSTEM_PROMPT = (
    "Ты — редактор и копирайтер, создающий вовлекающие тексты под заданный формат. "
    "Опирайся на нишу, цель и описание идеи, чтобы сделать пост полезным и структурированным."
)


def build_idea_prompt(niche: str, goal: str, content_format: str) -> str:
    return (
        "Сгенерируй ровно 5 различных идей для контента.\n"
        "Требования:\n"
        "1. Каждая идея должна учитывать нишу, цель и формат.\n"
        "2. Идеи должны отличаться подходом (например, разный угол, тон или тип подачи).\n"
        "3. Верни строго один JSON-массив. Элемент массива — объект c полями 'title' (краткий заголовок) и 'description' (2-3 предложения).\n"
        "4. Не добавляй никакого текста до или после JSON, в том числе пояснений."
        f"\n\nНиша: {niche}. Цель: {goal}. Формат: {content_format}."
    )


def build_post_prompt(
    niche: str,
    goal: str,
    content_format: str,
    idea_title: str,
    idea_description: str,
) -> str:
    return (
        "Напиши готовый текст на основе выбранной идеи.\n"
        "Требования:\n"
        "1. Первая строка должна быть жирным заголовком с эмодзи (например, `*🔥 Заголовок поста*`). Подзаголовки также выделяй жирным в формате `*Раздел:*`.\n"
        "2. Используй удобный Markdown для Telegram: списки оформляй маркерами '-', ключевые мысли подчёркивай жирным, избегай символов '#' и других заголовков, которые не поддерживаются.\n"
        "3. Сделай текст компактным и пригодным к публикации без доработок: короткие абзацы, обращение к читателю, логичный призыв к действию в конце.\n"
        "4. Используй данные о нише, цели, формате и описании идеи — пост должен отражать исходную задумку.\n"
        "5. Не добавляй пояснений до или после поста (например, 'Вот текст' или комментарии)."
        f"\n\nНиша: {niche}. Цель контента: {goal}. Формат: {content_format}."
        f"\nНазвание идеи: {idea_title}. Описание идеи: {idea_description}."
    )

